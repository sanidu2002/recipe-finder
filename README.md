# Recipe Finder

This repository is accessible and ready for course work.

## Quick setup

1. Create a virtual environment:
   ```bash
   python -m venv .venv
   source .venv/bin/activate
   ```
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
3. Run the app:
   ```bash
   python app.py
   ```
4. Open `http://127.0.0.1:5000` in your browser.

## Course work checklist

- [x] Flask app entry point (`app.py`) is present
- [x] Frontend template and static assets are present
- [x] Dependency file (`requirements.txt`) is present
- [x] Python cache files are ignored via `.gitignore`
